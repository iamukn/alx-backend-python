#!/usr/bin/env python3

"""Takes a list as an argument and returns their sum"""


def sum_list(input_list: list[float]) -> float:
    """sums the inpt_list and returns it"""

    return sum(input_list)
